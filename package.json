{
  "dependencies": {
    "axios": "^1.7.4",
    "multer": "^1.4.5-lts.1",
    "passport-google-oauth2": "^0.2.0"
  }
},
"scripts": {
	"test": "cd backend && npm test || exit 1 && cd ../frontend && npm test || exit 1"
}
